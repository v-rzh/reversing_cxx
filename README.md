# reversing_cxx
Some practice binaries for the C++ Reversing Basics lecture

## Building

Just run `make` in the repository root.
Modify the `CFLAGS` and `LDFLAGS` if you wish
to practice more difficult scenarios, for example
compile the bins with `-fno-rtti` and `-static`,
then strip the symbols.

Run `make clean` for cleanup.
